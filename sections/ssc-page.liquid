<article class="ssc-page">
  <header class="ssc-page-hero">
    <p class="ssc-eyebrow">{{ shop.name }}</p>
    <h1>{{ page.title }}</h1>
  </header>
  <div class="ssc-rte">
    {{ page.content }}
  </div>
</article>

{% schema %}
{
  "name": "SSC Page"
}
{% endschema %}
